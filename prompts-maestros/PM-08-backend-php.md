# PM-08 — Backend PHP

> Bloque 4 — Endpoint que devuelve productos en JSON.

```
Crea el archivo productos.php.

Requisitos:
- Conectar a MySQL con PDO
  - host: localhost
  - base: tienda_ia
  - usuario: root
  - password: vacío
- Hacer SELECT de productos activos con JOIN a categorías
- Devolver los datos como JSON
- Manejo de errores con try/catch
- Headers correctos: Content-Type: application/json
- CORS abierto para desarrollo: Access-Control-Allow-Origin: *

Devuelve solo el código PHP, sin explicaciones.
```
